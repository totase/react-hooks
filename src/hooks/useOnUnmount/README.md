# useOnUnmount

Trigger a callback function when the component is unmounted.

## Usage

```tsx
const Usage = () => {
  const handleUnmount = () => {
    ...
  };

  useOnUnmount(handleUnmount);

  ...
};
```
